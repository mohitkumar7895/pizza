/** Cart total must be at least this (₹) to place an order */
export const MIN_ORDER_AMOUNT = 150;

/** Show premium / priority messaging when total is above this (₹) */
export const PREMIUM_ORDER_THRESHOLD = 300;
